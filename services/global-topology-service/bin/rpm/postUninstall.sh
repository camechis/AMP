#!/bin/sh
script="gts"
serviceAccount="gts"
#if [ $1 = 0 ]; then # package is being erased
	# Any needed actions here on uninstalls
#else
	# Upgrade
#	/sbin/service $script restart > /dev/null 2>&1
#fi