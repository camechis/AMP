applicationContext: ['configuration/applicationContext.xml', 'configuration/inmemContext.xml', 'configuration/basicAuthSecurityContext.xml', 'configuration/snapshotContext.xml']

useSpringSecurity: true

logging:
    level: INFO
    console:
        enabled: true
        threshold: DEBUG
        
http:
    port: 15677
    adminUsername: admin
    adminPassword: admin123
    rootPath: /service/*
